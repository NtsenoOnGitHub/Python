# In python both double\single qoute can be used on string

imAstring = "Hello"
imAlsoAstring = 'World'
myAge = 26

print(imAstring, imAlsoAstring, f"I'm {myAge} years old")

#strings are an array of charecters and can be accessed via a [insex]

print(imAstring[0])

#Since strings are arrays, we can loop through the characters in a string, with a for loop

for x in imAstring:
    print(x)


